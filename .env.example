# ==============================================
# CINEGENY — Environment Variables
# ==============================================
# Copy this file to .env.local for local dev
# For Coolify: set these in the Coolify dashboard
# For Vercel: set these in Project Settings → Environment Variables
#             (see DEPLOY_VERCEL.md)
# ==============================================

# ---------- Database ----------
# Local Docker default:
DATABASE_URL="postgresql://cinegeny:cinegeny_prod_password@postgres:5432/cinegeny_db"
# Production (Neon / Vercel Postgres) — use the POOLED connection string, e.g.:
# DATABASE_URL="postgresql://USER:PASSWORD@ep-xxxx-pooler.eu-central-1.aws.neon.tech/cinegeny?sslmode=require"


# ---------- PostgreSQL container ----------
POSTGRES_USER="cinegeny"
POSTGRES_PASSWORD="cinegeny_prod_password"
POSTGRES_DB="cinegeny_db"

# ---------- NextAuth ----------
AUTH_SECRET="generate-with: openssl rand -base64 32"
AUTH_URL="https://platform.cinegeny.com"
NEXTAUTH_URL="https://platform.cinegeny.com"

# ---------- Redis (auto-configured in Docker) ----------
REDIS_URL="redis://redis:6379"

# ---------- Email (Resend) ----------
RESEND_API_KEY=""
RESEND_FROM_EMAIL="noreply@cinegeny.film"

# ---------- AI (Anthropic Claude) ----------
ANTHROPIC_API_KEY=""

# ---------- Payments (Stripe) — Lumen on/off-ramp (fiat, USD) ----------
# Lumens are the platform currency (1 Lumen = 1 USD). Stripe is used to BUY
# Lumens (on-ramp) and to PAY OUT withdrawals to a bank (off-ramp).
# Leave blank to run deposits/withdrawals in simulated mode.
STRIPE_SECRET_KEY=""
STRIPE_PUBLISHABLE_KEY=""
STRIPE_WEBHOOK_SECRET=""

# ---------- Identity verification (KYC) ----------
# Required once before a user can cash out (anti-money-laundering).
# Leave blank to auto-approve in simulated mode (for testing). e.g. "stripe-identity"
KYC_PROVIDER=""

# ---------- Crypto rails (optional, enable later) ----------
# Reserve backing the Lumen: USD + USDC stablecoin. Bitcoin is a payment method
# only (converted to Lumens on deposit), never held as reserve.
# Leave blank to keep crypto rails disabled.
USDC_PROVIDER_API_KEY=""
BITCOIN_PROVIDER_API_KEY=""

# ---------- Storage (S3-compatible: AWS S3 / Cloudflare R2 / Hetzner / MinIO) ----------
# Used for user uploads (avatars, films, audio). Leave blank to fall back to
# local/served uploads. Variable names must match exactly:
S3_ENDPOINT=""            # provider endpoint URL (omit for AWS S3; R2: https://<acct>.r2.cloudflarestorage.com)
S3_ACCESS_KEY_ID=""       # access key id
S3_SECRET_ACCESS_KEY=""   # secret access key
S3_BUCKET="cinegeny-uploads"  # bucket name (create it, allow public read for served files)
S3_REGION="eu-west-3"     # region (R2: "auto")

# ---------- Google Sign-In (OAuth) ----------
# Create an OAuth 2.0 Client ID (Web application) in Google Cloud Console.
# Authorized redirect URI: {AUTH_URL}/api/auth/callback/google
# Leave blank to hide/disable Google sign-in.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ---------- App ----------
NEXT_PUBLIC_APP_URL="https://platform.cinegeny.com"
NEXT_PUBLIC_APP_NAME="CINEGENY"
APP_PORT="3000"

# ---------- Deploy ----------
# Set to "true" on first deploy to seed the database with test data
SEED_DB="false"
